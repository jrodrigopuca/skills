# Data Model Local Specialization

Start from the shared template at `../../references/templates/data-model.md` and the shared conventions at `../../references/templates/common.md`.

Use this local note only for specialization:

- prefer schema and migration evidence over DTOs and transport types
- keep entity descriptions tied to business meaning, not only storage detail
- mark unclear cardinality as `Needs confirmation`
